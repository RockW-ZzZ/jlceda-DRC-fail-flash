# 1.2.0

## 新功能

1. 新增「定时保存」：可设置每 1 / 5 / 10 / 30 / 60 分钟自动保存当前激活文档（原理图 / PCB / 面板），支持开关
2. 新增「定时DRC检查」：可设置每 1 / 5 / 10 / 30 / 60 分钟自动执行 DRC 检查并语音播报，支持开关；可单独勾选是否呼出 EDA 自带 DRC 窗口
3. 定时设置自动记住（SYS_Storage），下次打开窗口自动恢复

# 1.1.0

## 新功能

1. 内置合成语音文件（`sch-error.wav`、`sch-warning.wav`、`pcb-error.wav`、`pcb-warning.wav`、`pass.wav`）随插件打包，导入即可使用，无需手动选择语音文件夹
2. 自定义语音优先于内置语音，未自定义的槽位自动使用内置语音，内置缺失时用系统语音合成（TTS）兜底
3. 检查 DRC 后自动呼出 EDA 自带 DRC 结果窗口展示明细

## 修正

1. 移除基于实时 DRC 的自动播报功能（该功能依赖 EDA v4.2+ API，当前 EDA v3 不支持）
2. 增强内置语音读取兼容性（兼容多种文件返回形态）

# 1.0.0

## 新功能

1. 原理图 / PCB DRC 检测完成后，按错误数、警告数播放对应语音提示
2. 支持自定义本地语音文件（`sch-error.wav`、`sch-warning.wav`、`pcb-error.wav`、`pcb-warning.wav`、`pass.wav`）
3. 未选择语音文件时使用系统语音合成（TTS）兜底
4. 支持播放倍速设置（0.5x / 0.75x / 1.0x / 1.25x / 1.5x / 2.0x）
5. 支持播放次数设置（按错误/警告数量、每类只播一次）
6. 语音文件持久化保存（IndexedDB），重新打开窗口自动加载
7. 无错误无警告时播报「咸鱼翻身啦！」通过提示音
